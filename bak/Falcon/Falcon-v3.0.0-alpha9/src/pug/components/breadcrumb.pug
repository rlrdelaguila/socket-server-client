extends ../layouts/LayoutComponent
include ../mixins/components/Breadcrumb

append variables
  - var firstParent = 'components'              
  - var currentPage = 'breadcrumb'
  -
    var pageHeader = {
      title: 'Breadcrumb',
      description: 'Indicate the current page’s location within a navigational hierarchy that automatically adds separators via CSS.',
      url: `https://v5.getbootstrap.com/docs/${bootstrapVersion}/components/breadcrumb/`,
      linkText: 'Breadcrumb on Bootstrap'
    }
 
block component
  +PageHeader(pageHeader)
  .card.mb-3
    .card-header
      +DocCardHeader('Example')
    .card-body.bg-light
      +componentCard
        +BreadcrumbExampleDemo